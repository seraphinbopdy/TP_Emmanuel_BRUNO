CREATE TABLE CHIEN (ID_CHIEN INTEGER NOT NULL, ID_CHENIL INTEGER, ID_PERSONNE INTEGER, NONCHIEN VARCHAR, PRIMARY KEY (ID_CHIEN))
CREATE TABLE PERSONNE (ID_PERSONNE INTEGER NOT NULL, AGEPERSONNE INTEGER, IDENTIFIANT INTEGER, NOMPERSONNE VARCHAR, PRENOMPERSONNE VARCHAR, ID_CHIEN INTEGER, PRIMARY KEY (ID_PERSONNE))
CREATE TABLE CHENIL (ID_CHENIL INTEGER NOT NULL, NOMCHENIL VARCHAR, PRIMARY KEY (ID_CHENIL))
CREATE TABLE PATHOLOGIE (ID_PATHO INTEGER NOT NULL, ID_CHIEN INTEGER, DESCRIPTIONPATHOLOGIE VARCHAR, NOMPATHOLOGIE VARCHAR, PRIMARY KEY (ID_PATHO))
ALTER TABLE CHIEN ADD CONSTRAINT FK_CHIEN_ID_CHENIL FOREIGN KEY (ID_CHENIL) REFERENCES CHENIL (ID_CHENIL)
ALTER TABLE PERSONNE ADD CONSTRAINT FK_PERSONNE_ID_CHIEN FOREIGN KEY (ID_CHIEN) REFERENCES CHIEN (ID_PERSONNE)
ALTER TABLE PATHOLOGIE ADD CONSTRAINT FK_PATHOLOGIE_ID_CHIEN FOREIGN KEY (ID_CHIEN) REFERENCES CHIEN (ID_PATHO)
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT NUMERIC(38), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 0)
